{
  "name": "@perfex/database",
  "version": "0.1.0",
  "private": true,
  "description": "Perfex database schemas and migrations",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate",
    "migrate:local": "wrangler d1 migrations apply perfex-db --local",
    "migrate:prod": "wrangler d1 migrations apply perfex-db",
    "studio": "drizzle-kit studio",
    "push": "drizzle-kit push"
  },
  "dependencies": {
    "drizzle-orm": "^0.37.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "drizzle-kit": "^0.29.1",
    "wrangler": "^4.50.0"
  }
}
